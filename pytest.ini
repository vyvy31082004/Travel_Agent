[pytest]
markers =
    live: live LLM/MCP smoke tests (require API keys and running MCP servers)
    e2e: live end-to-end agent cases (API + MCP + Postgres)
    multi: multi-domain E2E cases (two parallel domain branches)
